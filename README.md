<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dr. Mina Nabil — Elite Academic Consulting & Statistical Data Analysis</title>
    
    <meta name="description" content="Dr. Mina Nabil is an elite academic consultant specializing in Ph.D. dissertation writing, Master's thesis review, SPSS statistical data analysis, and peer-reviewed international journal publication support for postgraduate scholars.">
    <meta name="keywords" content="Elite Academic Consultant, Dissertation Data Analysis, SPSS Expert, PhD Thesis Formatting, Master's Thesis Consultant, Statistical Analysis R Python, Journal Publication Support, Academic Writing Services, Postgraduate Research Help, Data Analyst for Thesis">
    <meta name="author" content="Dr. Mina Nabil">
    <meta name="robots" content="index, follow">

    <meta property="og:type" content="website">
    <meta property="og:title" content="Dr. Mina Nabil — Elite Academic Consulting & Statistical Analysis">
    <meta property="og:description" content="World-class academic consulting for postgraduate scholars. Ph.D. dissertations, Master's theses, SPSS/R/Python data analysis, and international journal publication support.">
    <meta property="og:image" content="Gemini_Generated_Image_or792sor792sor79.png">

    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;500;600;700;800;900&family=Montserrat:wght@200;300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        /* --- ULTRA-LUXURY DESIGN SYSTEM --- */
        :root {
            --bg-pure: #050506;
            --bg-card: #0d0d11;
            --bg-card-hover: #14141a;
            --accent-bronze: #CD7F32;
            --accent-bronze-bright: #e5934c;
            --accent-bronze-glow: rgba(205, 127, 50, 0.15);
            --text-main: #ffffff;
            --text-muted: #8f8f9e;
            --text-silver: #d4d4d8;
            --glass-nav: rgba(5, 5, 6, 0.8);
            --transition-expensive: all 0.6s cubic-bezier(0.16, 1, 0.3, 1);
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            scroll-behavior: smooth;
        }

        body {
            background-color: var(--bg-pure);
            color: var(--text-main);
            font-family: 'Montserrat', sans-serif;
            overflow-x: hidden;
            -webkit-font-smoothing: antialiased;
            background-image: 
                linear-gradient(rgba(205, 127, 50, 0.015) 1px, transparent 1px),
                linear-gradient(90deg, rgba(205, 127, 50, 0.015) 1px, transparent 1px);
            background-size: 50px 50px;
            position: relative;
        }

        body::before {
            content: '';
            position: absolute;
            top: 20%;
            right: -10%;
            width: 600px;
            height: 600px;
            background: radial-gradient(circle, rgba(205, 127, 50, 0.04) 0%, transparent 70%);
            z-index: -1;
            pointer-events: none;
        }

        h1, h2, h3, .brand-font {
            font-family: 'Cinzel', serif;
            letter-spacing: 0.05em;
        }

        .bronze-text {
            background: linear-gradient(135deg, #ffffff 30%, var(--accent-bronze) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            font-weight: 800;
        }

        .section-title {
            font-size: 2.5rem;
            font-weight: 700;
            text-align: center;
            margin-bottom: 5rem;
            text-transform: uppercase;
            letter-spacing: 0.15em;
            position: relative;
        }

        .section-title::after {
            content: '';
            display: block;
            width: 50px;
            height: 2px;
            background: var(--accent-bronze);
            margin: 1.5rem auto 0 auto;
            opacity: 0.6;
        }

        .btn-luxury {
            display: inline-flex;
            align-items: center;
            gap: 1.2rem;
            background: transparent;
            color: #fff;
            border: 1px solid var(--accent-bronze);
            padding: 1.25rem 3rem;
            font-size: 0.8rem;
            font-weight: 600;
            letter-spacing: 0.25em;
            text-transform: uppercase;
            cursor: pointer;
            border-radius: 0px;
            transition: var(--transition-expensive);
            text-decoration: none;
            position: relative;
            box-shadow: inset 0 0 0 0 rgba(205, 127, 50, 0.2);
        }

        .btn-luxury:hover {
            background: var(--accent-bronze);
            color: var(--bg-pure);
            box-shadow: 0 20px 45px var(--accent-bronze-glow);
            transform: translateY(-5px);
            border-color: var(--accent-bronze-bright);
        }

        /* --- FIXED GLASS NAVBAR --- */
        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 1000;
            background: var(--glass-nav);
            backdrop-filter: blur(30px);
            -webkit-backdrop-filter: blur(30px);
            border-bottom: 1px solid rgba(205, 127, 50, 0.05);
            padding: 1.8rem 8%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: var(--transition-expensive);
        }

        .nav-logo {
            font-size: 1.25rem;
            font-weight: 700;
            color: #fff;
            text-decoration: none;
            letter-spacing: 0.25em;
        }

        .nav-links {
            display: flex;
            gap: 3.5rem;
            list-style: none;
        }

        .nav-links a {
            color: var(--text-muted);
            text-decoration: none;
            font-size: 0.75rem;
            font-weight: 500;
            text-transform: uppercase;
            letter-spacing: 0.2em;
            transition: var(--transition-expensive);
        }

        .nav-links a:hover {
            color: var(--accent-bronze);
            letter-spacing: 0.25em;
        }

        /* --- HERO SECTION --- */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            padding: 0 8%;
            position: relative;
            background: radial-gradient(circle at 80% 40%, #17120e 0%, var(--bg-pure) 60%);
            padding-top: 120px;
        }

        .hero-container {
            display: grid;
            grid-template-columns: 1.1fr 0.9fr;
            align-items: center;
            gap: 5rem;
            width: 100%;
        }

        .hero-tagline {
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 0.5em;
            color: var(--accent-bronze);
            margin-bottom: 2rem;
            display: block;
            font-weight: 600;
        }

        .hero-title {
            font-size: 4rem;
            line-height: 1.2;
            font-weight: 800;
            margin-bottom: 2.5rem;
            text-transform: uppercase;
        }

        .hero-description {
            font-size: 1.05rem;
            color: var(--text-muted);
            line-height: 1.9;
            margin-bottom: 3.5rem;
            max-width: 620px;
            font-weight: 300;
            letter-spacing: 0.02em;
        }

        .hero-portrait-container {
            position: relative;
            width: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .hero-portrait-frame {
            position: relative;
            border-radius: 4px;
            padding: 10px;
            background: linear-gradient(135deg, rgba(205,127,50,0.2) 0%, rgba(0,0,0,0) 60%);
            border: 1px solid rgba(205, 127, 50, 0.1);
            box-shadow: 0 50px 100px rgba(0,0,0,0.8);
        }

        .hero-img {
            max-width: 100%;
            height: auto;
            max-height: 520px;
            object-fit: cover;
            border-radius: 2px;
            display: block;
            filter: brightness(0.95) contrast(1.02);
            mask-image: linear-gradient(to bottom, black 85%, transparent 100%);
            -webkit-mask-image: linear-gradient(to bottom, black 85%, transparent 100%);
        }

        /* --- METRIC COUNTERS --- */
        .stats-section {
            background: #09090c;
            padding: 7rem 8%;
            border-top: 1px solid rgba(205, 127, 50, 0.05);
            border-bottom: 1px solid rgba(205, 127, 50, 0.05);
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 5rem;
            max-width: 1200px;
            margin: 0 auto;
            text-align: center;
        }

        .stat-number {
            font-size: 4.5rem;
            font-weight: 800;
            color: var(--accent-bronze);
            margin-bottom: 1rem;
            letter-spacing: -0.02em;
        }

        .stat-label {
            font-size: 0.75rem;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 0.25em;
            font-weight: 600;
        }

        /* --- SCROLL REVEALS ENGINE --- */
        .scroll-reveal {
            opacity: 0;
            transform: translateY(40px);
            transition: opacity 1.4s cubic-bezier(0.16, 1, 0.3, 1), transform 1.4s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .scroll-reveal.active {
            opacity: 1;
            transform: translateY(0);
        }

        .section-padding {
            padding: 12rem 8% 10rem 8%;
        }

        /* --- SERVICES GRIDS --- */
        .services-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 3.5rem;
            max-width: 1200px;
            margin: 0 auto;
        }

        .service-card {
            background: var(--bg-card);
            border: 1px solid rgba(255, 255, 255, 0.015);
            padding: 5rem 3.5rem;
            border-radius: 3px;
            transition: var(--transition-expensive);
            position: relative;
        }

        .service-card:hover {
            background: var(--bg-card-hover);
            transform: translateY(-12px);
            border-color: rgba(205, 127, 50, 0.35);
            box-shadow: 0 40px 70px rgba(0, 0, 0, 0.6), 0 0 35px var(--accent-bronze-glow);
        }

        .service-icon {
            font-size: 2.5rem;
            color: var(--accent-bronze);
            margin-bottom: 3rem;
            display: block;
        }

        .service-card h3 {
            font-size: 1.4rem;
            margin-bottom: 1.5rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        .service-card p {
            color: var(--text-muted);
            font-size: 0.9rem;
            line-height: 1.9;
            font-weight: 300;
        }

        /* --- PORTFOLIO MATRIX --- */
        .portfolio-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 4rem;
            max-width: 1200px;
            margin: 0 auto;
        }

        .portfolio-card {
            background: var(--bg-card);
            border-radius: 3px;
            border: 1px solid rgba(255, 255, 255, 0.015);
            transition: var(--transition-expensive);
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .portfolio-card:hover {
            border-color: rgba(205, 127, 50, 0.3);
            transform: translateY(-8px);
            background: var(--bg-card-hover);
            box-shadow: 0 40px 60px rgba(0,0,0,0.5), 0 0 30px var(--accent-bronze-glow);
        }

        .portfolio-body {
            padding: 4rem;
        }

        .portfolio-tag {
            font-size: 0.7rem;
            text-transform: uppercase;
            letter-spacing: 0.3em;
            color: var(--accent-bronze);
            margin-bottom: 2rem;
            display: inline-block;
            font-weight: 700;
        }

        .portfolio-title {
            font-size: 1.4rem;
            line-height: 1.6;
            margin-bottom: 2rem;
            font-weight: 600;
        }

        .portfolio-meta {
            background: rgba(205, 127, 50, 0.01);
            border-left: 2px solid var(--accent-bronze);
            padding: 1.8rem;
            font-size: 0.85rem;
            color: var(--text-silver);
            line-height: 1.8;
            font-weight: 300;
        }

        .portfolio-footer {
            padding: 2.5rem 4rem;
            background: rgba(0, 0, 0, 0.2);
            border-top: 1px solid rgba(255, 255, 255, 0.015);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .portfolio-link {
            color: #fff;
            text-decoration: none;
            font-size: 0.75rem;
            font-weight: 600;
            letter-spacing: 0.2em;
            text-transform: uppercase;
            display: inline-flex;
            align-items: center;
            gap: 0.8rem;
            transition: var(--transition-expensive);
        }

        .portfolio-link:hover {
            color: var(--accent-bronze);
            gap: 1.2rem;
        }

        /* --- INVESTMENT TIERS MATRIX --- */
        .pricing-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 2rem;
            max-width: 1200px;
            margin: 0 auto;
        }

        .pricing-card {
            background: var(--bg-card);
            border: 1px solid rgba(255, 255, 255, 0.015);
            border-radius: 3px;
            padding: 4.5rem 2.2rem;
            text-align: center;
            transition: var(--transition-expensive);
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .pricing-card:hover {
            border-color: rgba(205, 127, 50, 0.2);
            transform: translateY(-5px);
        }

        .pricing-card.featured {
            border-color: var(--accent-bronze);
            background: linear-gradient(180deg, #0d0d11 0%, #15110e 100%);
            transform: translateY(-10px);
            box-shadow: 0 30px 60px rgba(0,0,0,0.6);
        }

        .pricing-card.featured:hover {
            transform: translateY(-15px);
            box-shadow: 0 40px 70px rgba(0,0,0,0.7), 0 0 30px var(--accent-bronze-glow);
        }

        .pricing-name {
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 0.25em;
            color: var(--text-muted);
            margin-bottom: 2.5rem;
            font-weight: 700;
        }

        .pricing-price {
            font-size: 3rem;
            font-weight: 700;
            color: #fff;
            margin-bottom: 0.5rem;
            transition: opacity 0.25s ease-in-out, transform 0.25s ease-in-out;
        }

        .pricing-unit {
            font-size: 0.75rem;
            color: var(--text-muted);
            margin-bottom: 3rem;
            display: block;
            letter-spacing: 0.1em;
            text-transform: uppercase;
        }

        .pricing-features {
            list-style: none;
            text-align: left;
            margin-bottom: 3.5rem;
            display: flex;
            flex-direction: column;
            gap: 1.2rem;
            font-size: 0.85rem;
            color: var(--text-silver);
            font-weight: 300;
        }

        .pricing-features li {
            display: flex;
            align-items: center;
            gap: 0.8rem;
        }

        .pricing-features i {
            color: var(--accent-bronze);
            font-size: 0.75rem;
        }

        /* --- FORM REGISTRATION ENGINE --- */
        .booking-container {
            max-width: 850px;
            margin: 0 auto;
            background: var(--bg-card);
            padding: 6rem;
            border-radius: 3px;
            border: 1px solid rgba(255, 255, 255, 0.015);
            box-shadow: 0 50px 100px rgba(0,0,0,0.8);
        }

        .form-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 3rem;
            margin-bottom: 3.5rem;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 0.9rem;
        }

        .form-group.full-width {
            grid-column: span 2;
        }

        .form-label {
            font-size: 0.7rem;
            text-transform: uppercase;
            letter-spacing: 0.25em;
            color: var(--text-muted);
            font-weight: 700;
        }

        .form-control {
            background: #050507;
            border: 1px solid rgba(255, 255, 255, 0.06);
            padding: 1.4rem;
            color: #fff;
            font-family: inherit;
            font-size: 0.9rem;
            border-radius: 0px;
            transition: var(--transition-expensive);
        }

        .form-control:focus {
            outline: none;
            border-color: var(--accent-bronze);
            background: #0b0b0f;
            box-shadow: 0 0 20px rgba(205, 127, 50, 0.08);
        }

        select.form-control {
            appearance: none;
            -webkit-appearance: none;
            background-image: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='10' height='6' fill='none'><path stroke='%23CD7F32' stroke-width='1.5' d='M1 1l4 4 4-4'/></svg>");
            background-repeat: no-repeat;
            background-position: right 1.5rem center;
            padding-right: 3rem;
        }

        select.form-control option {
            background: #0d0d11;
            color: #fff;
        }

        .promo-status-box {
            background: rgba(0, 0, 0, 0.2);
            border: 1px dashed rgba(205, 127, 50, 0.15);
            padding: 2.5rem;
            border-radius: 0px;
            margin-bottom: 4rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .live-price-summary h4 {
            font-size: 0.7rem;
            text-transform: uppercase;
            color: var(--text-muted);
            letter-spacing: 0.15em;
            margin-bottom: 0.6rem;
        }

        .live-price-val {
            font-size: 2.4rem;
            font-weight: 700;
            color: var(--accent-bronze);
            transition: var(--transition-expensive);
        }

        .promo-msg {
            font-size: 0.75rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.15em;
            transition: var(--transition-expensive);
        }

        .promo-success { color: #34d399; }
        .promo-neutral { color: var(--text-muted); }

        .btn-submit {
            width: 100%;
            justify-content: center;
        }

        /* --- FOOTER PLATFORM --- */
        footer {
            background: #030304;
            padding: 6rem 8% 5rem 8%;
            text-align: center;
            border-top: 1px solid rgba(255, 255, 255, 0.015);
            font-size: 0.75rem;
            color: var(--text-muted);
            letter-spacing: 0.1em;
        }

        .footer-socials {
            display: flex;
            justify-content: center;
            gap: 3rem;
            margin-bottom: 3rem;
        }

        .footer-socials a {
            color: var(--text-muted);
            font-size: 1.3rem;
            transition: var(--transition-expensive);
        }

        .footer-socials a:hover {
            color: var(--accent-bronze);
            transform: translateY(-3px);
        }

        /* --- RESPONSIVE ADAPTATIONS --- */
        @media (max-width: 1200px) {
            .pricing-grid { grid-template-columns: repeat(2, 1fr); gap: 2.5rem; }
        }

        @media (max-width: 1024px) {
            .hero-container { grid-template-columns: 1fr; text-align: center; gap: 4rem; }
            .hero-description { margin: 0 auto 3.5rem auto; }
            .services-grid, .portfolio-grid { grid-template-columns: 1fr; max-width: 700px; }
            .hero-portrait-container { order: -1; max-width: 420px; margin: 0 auto; }
            .hero-title { font-size: 3.2rem; }
            .section-title { font-size: 2.2rem; }
        }

        @media (max-width: 768px) {
            .hero-title { font-size: 2.4rem; }
            .stats-grid { grid-template-columns: 1fr; gap: 4rem; }
            .pricing-grid { grid-template-columns: 1fr; max-width: 420px; }
            .form-grid { grid-template-columns: 1fr; gap: 2rem; }
            .form-group.full-width { grid-column: span 1; }
            .booking-container { padding: 4rem 2rem; }
            .nav-links { display: none; }
        }
    </style>
</head>
<body>

    <nav class="navbar">
        <a href="#" class="nav-logo">DR. MINA NABIL</a>
        <ul class="nav-links">
            <li><a href="#services">Services</a></li>
            <li><a href="#portfolio">Showcase</a></li>
            <li><a href="#pricing">Pricing</a></li>
            <li><a href="#book">Consultation</a></li>
        </ul>
        <a href="#book" class="btn-luxury" style="padding: 0.75rem 2rem; font-size: 0.7rem; letter-spacing: 0.15em;">Inquire</a>
    </nav>

    <section class="hero">
        <div class="hero-container">
            <div class="hero-content">
                <span class="hero-tagline">Where Scholarship Meets Precision</span>
                <h1 class="hero-title">From First Hypothesis<br>To <span class="bronze-text">Published Excellence.</span></h1>
                <p class="hero-description">Meticulous academic consulting, advanced statistical analysis, and world-class thesis architecture engineered for the most ambitious postgraduate minds globally.</p>
                <a href="#book" class="btn-luxury">Secure Elite Consultation <i class="fa-solid fa-chevron-right" style="font-size: 0.7rem;"></i></a>
            </div>
            <div class="hero-portrait-container">
                <div class="hero-portrait-frame">
                    <img src="Gemini_Generated_Image_or792sor792sor79.png" alt="Dr. Mina Nabil Portrait Portfolio" class="hero-img">
                </div>
            </div>
        </div>
    </section>

    <section class="stats-section">
        <div class="stats-grid">
            <div class="stat-item">
                <div class="stat-number brand-font" data-target="250">0</div>
                <div class="stat-label">Theses Formatted & Consulted</div>
            </div>
            <div class="stat-item">
                <div class="stat-number brand-font" data-target="65">0</div>
                <div class="stat-label">International Journal Publications</div>
            </div>
            <div class="stat-item">
                <div class="stat-number brand-font" data-target="4">0</div>
                <div class="stat-label">Years of Elite Consulting Experience</div>
            </div>
        </div>
    </section>

    <section id="services" class="section-padding scroll-reveal">
        <h2 class="section-title">Academic <span class="bronze-text">Service Tiers</span></h2>
        <div class="services-grid">
            <div class="service-card">
                <i class="fa-solid fa-graduation-cap service-icon"></i>
                <h3 class="brand-font">Graduation Projects</h3>
                <p>Complete layout structure, deep academic literature reviews, precise guideline alignments, and pristine presentation assets for final undergraduate theses.</p>
            </div>
            <div class="service-card">
                <i class="fa-solid fa-book-bookmark service-icon"></i>
                <h3 class="brand-font">Master's & Ph.D. Theses</h3>
                <p>End-to-end scholarly consulting. Robust methodology layout development, deep hypothesis testing, and rigorous analytical verification matching international boards.</p>
            </div>
            <div class="service-card">
                <i class="fa-solid fa-chart-line service-icon"></i>
                <h3 class="brand-font">Advanced Analytics</h3>
                <p>Flawless data computations using SPSS, R, or Python. Beautiful chart vector generation, cross-tabulation metrics, and deep scientific discussion writing.</p>
            </div>
        </div>
    </section>

    <section id="portfolio" class="section-padding scroll-reveal" style="background: #09090c;">
        <h2 class="section-title">Analytical <span class="bronze-text">Showcase Matrix</span></h2>
        <div class="portfolio-grid">
            
            <div class="portfolio-card">
                <div class="portfolio-body">
                    <span class="portfolio-tag">Surgical & Clinical Analytics</span>
                    <h3 class="portfolio-title brand-font">Outcomes of Clinical and Surgical Assessment of Women with Pathological Nipple Discharge</h3>
                    <div class="portfolio-meta">
                        <strong>Statistical Parameters:</strong> Diagnostic accuracy verification, cross-tabulation indices, demographic outcome variance, and predictive clinical mapping.
                    </div>
                </div>
                <div class="portfolio-footer">
                    <span style="font-size: 0.75rem; color: var(--text-muted); font-weight: 5
